# 다음 단계 가이드

## ✅ 완료된 작업

1. **Supabase 설정**
   - 프로젝트 생성 및 연결
   - 데이터베이스 스키마 적용
   - RLS 정책 적용
   - 타입 생성

2. **Next.js 기본 구조**
   - Next.js 14+ 설치
   - 기본 페이지 생성 (홈, 온보딩, Writing)
   - 레이아웃 및 스타일 설정
   - PWA 설정

## 🚀 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속하여 확인하세요.

## 📋 다음 구현 항목

### 1. 게스트 모드 인증 (우선순위: 높음)
- `app/api/auth/guest/route.ts` 생성
- 익명 계정 생성 로직
- 세션 관리

### 2. 한글 문장 생성 기능
- `app/api/study/generate-mission/route.ts` 생성
- OpenAI API 연동
- 수준별 문장 생성

### 3. Writing 제출 및 AI 피드백
- `app/api/study/submit/route.ts` 생성
- 3중 필터링 파이프라인 구현
- AI 피드백 생성 및 저장

### 4. Queue 시스템
- 무료 유저 일일 제한 처리
- Queue 상태 관리

### 5. 리포트 기능
- 주간 통계
- 단어 히트맵
- 성취도 분석

## 📁 프로젝트 구조

```
app/
├── layout.tsx          # 루트 레이아웃
├── page.tsx            # 홈 페이지
├── globals.css         # 글로벌 스타일
├── onboarding/         # 온보딩 페이지
│   └── page.tsx
├── writing/            # Writing 페이지
│   └── page.tsx
└── api/                # API Routes
    ├── auth/
    └── study/

lib/
└── supabase/           # Supabase 유틸리티
    ├── client.ts
    ├── server.ts
    └── utils.ts

types/
└── database.ts         # Supabase 타입 정의
```

## 🔧 개발 팁

1. **타입 안전성**: `types/database.ts`의 타입을 활용하세요
2. **Supabase 사용**: `lib/supabase/server.ts`의 헬퍼 함수 사용
3. **환경 변수**: `.env.local` 파일 확인
4. **RLS 정책**: 모든 쿼리는 RLS 정책을 따릅니다

## 📚 참고 문서

- `SUPABASE_SETUP.md` - Supabase 설정 가이드
- `lib/supabase/README.md` - Supabase 유틸리티 사용법
- `docs/spec.md` - 프로젝트 스펙
- `docs/03_design.md` - 시스템 설계 문서
